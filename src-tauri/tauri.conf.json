{
  "productName": "rj-player",
  "version": "0.2.0",
  "identifier": "io.github.karta0898098.rjplayer",
  "build": {
    "frontendDist": "../frontend/dist",
    "beforeDevCommand": "bash -c 'export NVM_DIR=\"$HOME/.nvm\"; [ -s \"$NVM_DIR/nvm.sh\" ] && . \"$NVM_DIR/nvm.sh\"; cd \"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\" && bash src-tauri/binaries/fetch-sidecars.sh && npm --prefix frontend run build'",
    "beforeBuildCommand": "bash -c 'export NVM_DIR=\"$HOME/.nvm\"; [ -s \"$NVM_DIR/nvm.sh\" ] && . \"$NVM_DIR/nvm.sh\"; cd \"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\" && bash src-tauri/binaries/fetch-sidecars.sh && npm --prefix frontend run build'"
  },
  "app": {
    "windows": [],
    "macOSPrivateApi": true,
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "externalBin": ["binaries/yt-dlp", "binaries/ffmpeg", "binaries/uv"],
    "resources": {
      "../frontend/dist": "frontend/dist",
      "../ai/worker.py": "ai/worker.py",
      "../ai/requirements.txt": "ai/requirements.txt",
      "../ai/requirements-cuda.txt": "ai/requirements-cuda.txt",
      "../ai/requirements-demucs.txt": "ai/requirements-demucs.txt",
      "../ai/pipeline": "ai/pipeline"
    },
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ],
    "android": {
      "debugApplicationIdSuffix": ".debug"
    }
  }
}
