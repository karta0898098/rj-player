faster-whisper==1.2.1
# faster-whisper's native deps are unpinned upstream, so a resolve done fresh
# on an end-user machine can land on a ctranslate2/onnxruntime/numpy
# combination that isn't actually compatible (observed: Doctor's install_deps
# step failing with a bare "exited with status 2", no readable cause without
# re-running by hand). Pin to the versions verified working in the repo venv.
ctranslate2==4.8.1
onnxruntime==1.27.0
numpy==2.4.6
fugashi[unidic-lite]==1.5.2
pykakasi==2.3.0
anthropic==0.117.0
google-genai==2.12.1
openai>=1.40
stable-ts==2.19.1
# stable-ts -> openai-whisper -> numba: without this pin the resolver picks an
# ancient numba 0.53 / llvmlite 0.36 that fails to build on Python 3.12. Pin to a
# 3.12-compatible numba (matches the versions the repo venv runs on: numba 0.66,
# llvmlite 0.48).
numba>=0.60
